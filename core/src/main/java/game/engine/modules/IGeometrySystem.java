package game.engine.modules;

public interface IGeometrySystem extends IGeometryModule, IGeometryFactory {
    // This interface combines the functionalities of GeometryModule and GeometryFactory.
    // It allows for both querying entities in a specific area and creating new geometry representations.

    // No additional methods are required here as it inherits all necessary methods from GeometryModule and GeometryFactory.
}
