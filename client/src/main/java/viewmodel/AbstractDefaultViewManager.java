package viewmodel;

            //don't use outside package concerning frontend. use AbstractViewManager instead
public interface AbstractDefaultViewManager extends AbstractViewManager {
    void start();
}
